/* Метод slice() извлекает часть строки и возвращает новую строку без изменения оригинальной строки.*/
// Принимает до двух параметров positive & negative

let text="Bon aqua"
console.log(text.slice(-6,-2)); // n qa