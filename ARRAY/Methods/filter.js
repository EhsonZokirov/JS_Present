// let arr = ['hello', 'disco', 'love']
// let arr2 = arr.filter(arr => arr.length > 4)
// console.log(arr2);

let arr = [1, 2, 3, 4, 5]
let arr2 = arr.filter(arr => arr > 2)
console.log(arr2);

/* Метод filter() создаёт новый массив со всеми элементами, 
прошедшими проверку, задаваемую в передаваемой функции.*/